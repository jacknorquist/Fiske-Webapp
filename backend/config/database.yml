# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  adapter: postgresql
  # encoding: unicode
  database: fiske
  # pool: 5
  # username: jacknorquist
  password: <%= ENV['local_key'] %>
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: db/proj_test.sqlite3

production:
  <<: *default
  adapter: postgresql
  password: <%= ENV['prod_key'] %>
  database: postgres://ztxaldlq:M42-RaZNPPtrrodVz21ZQPPN1FVmdXZR@bubble.db.elephantsql.com/ztxaldlq
